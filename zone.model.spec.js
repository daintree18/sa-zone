'use strict';

describe('Model: Zone', function ()
{
	// Load the common tests
	window.specs.run();

	// Instantiate service
	beforeEach(inject(function ()
	{
	}));
	
	it('should be true', function (Zone)
	{
	});
});
